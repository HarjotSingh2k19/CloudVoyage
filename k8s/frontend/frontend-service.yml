apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: cloudvoyage
spec:
  type: NodePort  # Allows external access
  selector:
    app: frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80